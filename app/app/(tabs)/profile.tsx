import { StyledView, StyledText } from '../../App';

export default function ProfileScreen() {
  return (
        <StyledView>
        <StyledText className='text-white'>
            Hello User! Welcome to profile
        </StyledText>
        </StyledView>
  );
}
